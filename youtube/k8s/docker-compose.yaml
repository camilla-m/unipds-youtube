services:
  web:
    image: my-api:latest
    ports:
      - "8080:8080"
    environment:
      - DB_URL=db-service
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
  db:
    image: postgres:15
    volumes:
      - db-data:/var/lib/postgresql/data